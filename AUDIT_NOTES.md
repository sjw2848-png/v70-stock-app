# V78.6.1 Audit Notes

- 개인 계정 ID 변경 API/화면 추가: 기존 보유·관심·모으기 서버 자료를 새 ID 저장공간으로 이전합니다.
- 비밀번호 변경 API/화면 추가: 현재 비밀번호 인증 후 새 PBKDF2-SHA256 salt/hash로 교체합니다.
- 로그아웃 버튼 추가.
- 계정 비밀번호는 더 이상 localStorage 설정에 저장하지 않고 현재 브라우저 세션의 sessionStorage에만 보관합니다.
- ID 변경 시 기기 로컬 포트폴리오 키도 새 ID로 이전합니다.
- V78.6.1 버전/manifest/PWA cache 명칭 통일.

## Previous V78.6.0 notes

- 개인 동기화 ID를 **개인 계정 ID + 비밀번호** 구조로 강화했습니다.
- 같은 서버를 여러 사용자가 사용해도 계정별 포트폴리오 저장 공간이 분리됩니다.
- 서버에는 비밀번호 원문을 저장하지 않고 PBKDF2-SHA256(개별 salt, 180,000회) 해시만 저장합니다.
- 개인 계정 연결 전에는 보유/관심/모으기 종목을 화면에 노출하지 않습니다.
- 브라우저 로컬 저장도 계정 ID별 키로 분리하여 같은 기기에서 계정을 바꿔도 다른 계정의 종목이 자동 노출되지 않도록 했습니다.
- 새 계정 연결 후 사용자가 “기존 내 자료 가져오기”를 눌렀을 때만 같은 브라우저의 V78.3~V78.5 자료와 기존 동기화 ID의 서버 자료를 현재 계정으로 병합 이관합니다. 자동 이관은 하지 않아 다른 사용자의 자료가 새 계정에 섞이는 위험을 줄였습니다.
- 휴대폰과 PC는 같은 계정 ID/비밀번호로 연결하면 같은 서버 포트폴리오를 사용합니다.
- 배포 시 DATA_DIR는 반드시 영구 디스크(/var/data 등)를 사용해야 서버 재시작 후 계정/포트폴리오가 유지됩니다.
- 계정 비밀번호 분실 복구 기능은 아직 없습니다. 비밀번호를 잊으면 해당 계정 데이터에 접근할 수 없습니다.
- 이 앱은 투자 판단 보조 도구이며 실제 수익을 보장하지 않습니다.
